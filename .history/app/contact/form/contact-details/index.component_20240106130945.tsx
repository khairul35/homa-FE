import { ComponentProps } from "../index.props";

const Component = (props: ComponentProps) => {
    const { contact } = props;

    return (
        <div>

        </div>
    );
};

export default Component;
